# ════════════════════════════════════════════════════════════════
# 条件刷新测试验证模块 —— 测试专用配置
#
# 使用 main 的 bootstrap.yml（通过 Maven 资源过滤注入 Nacos 地址），
# 仅覆盖 application 层配置项。
# ════════════════════════════════════════════════════════════════

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

conditional:
  refresh:
    enabled: true
    debounce:
      delay: 100
    metrics-enabled: true
    initial-snapshot-enabled: true

channel:
  sign:
    secret: "init-secret-value"
    token: "init-token-value"
template:
  max:
    retry: 3
  timeout: 5000
custom:
  feature:
    enabled: false

logging:
  level:
    com.liu.conditionalrefresh: DEBUG
    com.liu.conditionalrefresh.test: DEBUG

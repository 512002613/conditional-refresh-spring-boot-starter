# ════════════════════════════════════════════════════════════════
# 条件刷新测试验证模块 — Spring Boot 4.0.x
# ════════════════════════════════════════════════════════════════

server:
  port: 8084

spring:
  application:
    name: conditional-refresh-test-sample-v4
  config:
    import: nacos:
  cloud:
    bootstrap:
      enabled: true
    nacos:
      config:
        server-addr: @nacos.server-addr@
        namespace: @nacos.namespace@
        group: @nacos.group@
        file-extension: yaml
        enabled: true
        shared-configs:
          - data-id: conditional-refresh-test-sample-v4
            group: @nacos.group@
            refresh: true
          - data-id: conditional-refresh-test-sample-v4.yaml
            group: @nacos.group@
            refresh: true
      discovery:
        enabled: false

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

conditional:
  refresh:
    enabled: true
    debounce:
      delay: 500
    metrics-enabled: true
    initial-snapshot-enabled: true

channel:
  sign:
    secret: "default-secret"
    token: "default-token"
template:
  max:
    retry: 3
  timeout: 5000
custom:
  feature:
    enabled: false

logging:
  level:
    com.liu.conditionalrefresh: DEBUG
    com.liu.conditionalrefresh.test: DEBUG
